Title: chore(skills): normalize SKILL frontmatter, redact examples, add validators and CI

Summary:
This PR centralizes and hardens the `skills/` and `agents/` content:
- Normalized YAML frontmatter across `skills/**/SKILL.md` and added `owner` fields.
- Redacted secret-like data inside fenced code blocks and normalized absolute paths to `${PROJECT_ROOT}` where appropriate.
- Added validators: `tools/validate_skills.py` and `tools/validate_agents.py` and unit tests.
- Added CI workflow `.github/workflows/agents-ci.yml` to run validators and tests.
- Added `agents/` folder with persona templates and `agents/README.md`.
- Added helper scripts: `tools/fix_skill_paths.py`, `tools/redact_skill_secrets.py`, `tools/finalize_paths.py`, `tools/audit_skills.py` and `tools/set_owner.py`.
- Added `REPORTS/skills_validation_report.txt` and `REPORTS/skills_audit.csv` for reviewer reference.

What to review:
- Verify `skills/**/SKILL.md` content still conveys the intended guidance; some examples were normalized and may need manual rewording where pedagogy matters.
- Review `owner:` assignments (currently set to the committer email `noname.spyware@gmail.com`). Adjust to proper GitHub handles or team aliases if desired.
- Check remaining validator warnings (non-blocking) listed in `REPORTS/skills_validation_report.txt` — these are illustrative absolute-path examples left as warnings.
- Confirm CI workflow and validate no sensitive data was removed from examples that should remain.

Checklist before merge:
- [ ] Approve `owner` mappings or request changes (I can update owners programmatically).
- [ ] Decide policy for remaining absolute-path examples (replace with `${PROJECT_ROOT}` or add rationale comments).
- [ ] Optional: update `allowedTools` per skill/persona (currently defaults or not set).
- [ ] Run a manual pass over sample SKILLs to ensure examples still teach the intended workflow.

Notes for maintainers:
- Use `tools/validate_skills.py` locally to validate changes before pushing.
- To set owner programmatically: `python tools/set_owner.py "you@yourorg.com"`.
- To regenerate reports: `python tools/audit_skills.py` and `python tools/validate_skills.py > REPORTS/skills_validation_report.txt`.

Remaining work (recommended follow-ups):
- Tune `allowedTools` per agent and add governance for tool usage.
- Add `pre-commit` hooks to run validators (this PR includes a pre-commit config).
- Expand unit tests to cover validator edge cases.

Generated by automation. If you want more narrow edits (e.g., preserve specific examples), request files to revert or annotate.
